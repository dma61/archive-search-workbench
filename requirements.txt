# hdd-indexer (Archive Search Workbench) — Python runtime-afhankelijkheden.
# Versies gepind vanuit de draaiende de server-venv (reproduceerbaar).
# Webapp (web_app.py): Flask + PyYAML. Scripts/metadata: PyMuPDF, openpyxl, python-docx.

Flask==3.1.3
PyYAML==6.0.3
PyMuPDF==1.27.2.3
openpyxl==3.1.5
python-docx==1.2.0

# Niet-pip systeemtools (apt), buiten dit bestand: recoll, ripgrep, exiftool, mediainfo,
# smartmontools, poppler-utils, p7zip-full, unrar, sqlite3, ntfs-3g, udisks2, usbip.
